-- config.lua

-- Configuration settings
local config = {
    windowWidth = 1920,
    windowHeight = 1080,
    windowFlags = {
        resizable = false,
        vsync = true,
        fullscreen = false,
        centered = true
    }
}

return config

