-- config.lua

-- Configuration settings
local config = {
    windowWidth = 1280,
    windowHeight = 720,
    windowFlags = {
        resizable = false,
        vsync = true,
        fullscreen = false,
        centered = true
    }
}

return config

